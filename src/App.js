export default function Square() {// our first component- is a piece of reusable code that shows on the screen
 // export makes function accessible outisde of file, default means main function
  return <button className="square">X</button>;// export
}
